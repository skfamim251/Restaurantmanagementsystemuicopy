
  # Restaurant Management System UI (Copy)

  This is a code bundle for Restaurant Management System UI (Copy). The original project is available at https://www.figma.com/design/cUrkXsyScONJvx6Na0LLqQ/Restaurant-Management-System-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  